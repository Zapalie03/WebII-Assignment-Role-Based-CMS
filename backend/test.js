const express = require("express");  
const app = express();  
app.get("/", (req, res) = test: "ROOT" }));  
app.get("/api", (req, res) = test: "API" }));  
app.get("/health", (req, res) = test: "HEALTH" }));  
app.listen(5000, () =
